import styled from "styled-components";

const Container = styled.div`
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 20px;
    padding: var(--padding);
    /* max-width: 1440px; */
`;

export { Container }